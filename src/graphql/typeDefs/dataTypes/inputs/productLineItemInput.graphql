input ProductLineItemInput {
  "The product id for this line item"
  productId: ID!
  "the quantity of items of productId being sold"
  quantity: Int!
  "Denotes if this line item is bulk or individual only. Drives auto-allocation"
  isBulkPurchase: Boolean!
}
